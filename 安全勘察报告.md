# SmartEnglish Desktop 安全与质量勘察报告

> 勘察对象：`E:\YINGYU\YINGYU\smartenglish-desktop`
> 勘察时间：2026-08-22
> 勘察角色：代码/安全勘察员（仅勘察，不修复）
> 勘察性质：静态阅读源码 + 配置 + 仓库结构，无运行、无动态测试

---

## 0. 项目概览

| 项 | 内容 |
|----|------|
| 类型 | Flutter（Windows / Android）桌面与移动英语学习应用 |
| 体量 | Dart 源码约 39,600 行（其中 `lib/state.dart` 单文件 7,449 行） |
| 关键能力 | AI 智能出题 / 对话助手（Agent）/ 词汇剖析 / 模拟考试 / 墨墨背单词同步 / 贪吃蛇等游戏 / 内置浏览器 / MCP 客户端 |
| 敏感依赖 | `flutter_inappwebview`（WebView2）、`http`、`shared_preferences`、`dart:ffi`、`file_picker`、`desktop_drop` |
| 子目录异常 | 根目录含 `deepseek-harness/`（大型 TS/Node 仓库，非 Flutter 子模块）、`racing-game/`、`snake-game/`、`tool/`、`installer/`、`native/` |
| 仓库规模 | 仅 158 个文件被 git 跟踪（`node_modules`/`dist`/DLL 等未被提交，`.gitignore` 基本生效） |

**结论先行**：未发现硬编码的密钥/令牌泄漏、未发现恶意依赖或后门。但存在**一类高风险的"设计性"攻击面**——一个拥有文件读取/命令执行能力的 AI Agent 工具链，叠加**密钥明文存储**，在"提示词注入 + 默认权限"组合下即可形成数据泄露乃至远程代码执行的现实路径。此外有多个中等风险的实现与卫生问题。

---

## 1. 高风险（High）

### 1.1 API Key / Token 明文存储，且备份导出同明文
- **位置**：`lib/services/storage.dart`
  - `saveApiConfig`（L74-83）：`apiKey` 经 `_set` 写入 `shared_preferences`（明文）
  - `saveChatConfig`（L140-147）：`chatApiKey` 明文
  - `saveSearchKey`（L180）：`searchKey` 明文
  - `saveMaimemoToken`（L184）：墨墨 Bearer Token 明文
  - `buildBackupJson`（L505-560）与 `importBackup`（L563-621）：**把上述全部密钥原样写进备份 JSON 并支持导入**
- **影响**：`shared_preferences` 在 Windows 上以明文 JSON/XML 落盘于 `C:\Users\<user>\AppData\Local\…` 下（处于默认 fs 根 `c:\users` 子树内，见 1.2）。任意能读该文件的其他进程/用户、或一份通过聊天分享的"备份文件"，即可直接拿到用户的 LLM API Key 与墨墨 Token。
- **风险**：凭据泄露（机密性），且备份导入无任何来源校验，可覆盖本地配置。
- **建议**：密钥改用系统凭据库（Windows Credential Manager / `flutter_secure_storage`）、或至少加密后落盘；备份导出时对密钥做掩码/加密，导入时校验签名。

### 1.2 AI Agent 工具链 + 提示词注入 → 数据泄露 / 远程代码执行路径
- **位置**：`lib/services/agent_service.dart`（工具定义，30+ 个工具）、`lib/state.dart` 的 `executeTool` / `_isDangerousTool`（L2331-2496）
- **高危工具**（受 `chatFullAccess` 开关与 `_isDangerousTool` 门控）：
  - `bash`（L537）：Windows `cmd /c` 执行任意命令（`state.dart` L3461，`Process.run('cmd', ['/c', cmd])`，无路径/命令限制）
  - `operate_computer`（L346）：`open_file/open_folder/open_url/launch_app/run_command`
  - `write_file` / `edit_file` / `config_settings(set)`
  - `call_mcp_tool` / `mcp__*`：可驱动任意 MCP server（见 2.5）
  - `run_code`：解析并执行模型编写的 `tools.xxx(...)` 调用序列（`state.dart` L4006）
- **默认权限（沙箱）模型**：
  - `_isDangerousTool`（L2331）拦截 `operate_computer / write_file / edit_file / bash / config_settings-set`，默认 `chatFullAccess=false` → 这些被禁。
  - 但 `read_file` / `list_dir` / `run_code`（仅调非危险工具）**在默认模式下即被允许**，且受 `_fsRoot()` 约束在 `c:\users`（默认）子树。**这意味着 Agent 默认即可读取整个用户目录**，包含 1.1 中存放 API Key 的 `shared_preferences` 文件、文档、桌面等。
- **注入面**：`web_fetch`（L4172）、`search_web`、`call_mcp_tool` 的返回内容会直接进入 LLM 上下文。若抓取/检索到被植入指令的网页或恶意 MCP 返回，模型可被诱导：
  1. 默认模式下：`read_file` 读取 `shared_preferences` → 拿到 API Key → `web_fetch` 外发到攻击者服务器（**无需开启"完全访问"即可完成凭据外泄**）；
  2. 开启"完全访问"后：`bash`/`run_command` 直接在本机执行任意命令（典型 RCE）。
- **风险**：高。属"工具增强型 LLM 的提示词注入"经典攻击面；默认模式的读取半径过大进一步放大了危害。
- **建议**：
  - 收紧默认 fs 根（不要默认开放整个 `c:\users`，至少排除 `AppData`/凭据路径）；
  - 对 `web_fetch`/`search_web` 返回内容做"不可信数据"标注与指令隔离（不在系统提示中直接信任其文本）；
  - 考虑把 `bash`/文件写等纳入显式用户确认而非仅靠开关；
  - 富化审计日志（`recentToolCalls` 已记录，建议落地导出与告警）。

### 1.3 安装程序要求管理员权限 + 提交未签名预构建安装包
- **位置**：`installer/afloat_setup.iss`
  - `PrivilegesRequired=admin`（L27）：安装需提权
  - `AppId={{A1B2C3D4-E5F6-7890-ABCD-EF1234567890}`（L12）：明显占位 GUID
  - `AppPublisherURL/AppUpdatesURL=https://github.com`（L7-18）：占位，无真实更新/发布地址
  - 无 Authenticode 签名配置
- **仓库侧**：`git ls-files` 显示 `afloat-setup.exe` 与 `installer/output/afloat-setup.exe` 被跟踪（属 `git add -f` 强制加入，绕过 `.gitignore` 的 `*.exe`）。
- **影响**：
  - 普通学习软件要求管理员安装，扩大安装期攻击面（如安装目录/DLL 种植场景）；
  - 预构建、未签名安装包被提交进仓库：若构建机或仓库被篡改，用户将拿到恶意安装包（供应链风险）；
  - 占位 GUID 可能导致与其他软件冲突或卸载异常。
- **建议**：非必要不要求 admin；安装包应签名并从可信通道分发；不要将预构建 exe 提交进版本库（用 CI 产出）。

---

## 2. 中风险（Medium）

### 2.1 内置 WebView 浏览器：JS 全开、无 URL 限制、控制器未释放
- **位置**：`lib/widgets/browser_page.dart`
  - `InAppWebViewSettings`：`javaScriptEnabled: true`、`javaScriptCanOpenWindowsAutomatically: true`、`mediaPlaybackRequiresUserGesture: false`（L34-42）
  - 地址栏对无 scheme 输入自动补 `http://` 或 `https://`（L59-66），**无任何域名白名单/黑名单**，`useShouldOverrideUrlLoading: false`
  - `dispose()`（L46-50）只释放 `TextEditingController` / `FocusNode`，**未释放 `InAppWebViewController _webCtrl`**（旧版插件可能残留 Web 进程/缓存）
- **影响**：一个 JS 全开、可访问任意网址的通用浏览器被内置进学习软件，可被用于钓鱼、加载恶意内容；资源未释放可能泄漏内存/会话。
- **建议**：按需收紧 JS/弹窗；对可访问域名做策略；在 `dispose` 中调用 `webCtrl.dispose()`（视插件版本）。

### 2.2 `web_fetch` 存在 SSRF（无内网/回环地址限制）
- **位置**：`lib/state.dart` `_toolWebFetch`（L4172-4208）
  - 仅校验 scheme 为 `http(s)`（L4175），**未禁止 `localhost`、`127.0.0.1`、`169.254.169.254`（云元数据）等私有/链路本地地址**
  - `HttpClient` 仅设连接超时 10s、读取 30s（L4182-4185），无总超时上限的额外防护
- **影响**：Agent（默认即可调用 `web_fetch`）可被诱导访问内网服务或云元数据接口，造成内网探测/凭据泄露（配合 1.2 可外发结果）。
- **建议**：对目标地址做 DNS 解析后拒绝私有/回环/链路本地范围；或限制为公开外网并加请求总超时与大小上限。

### 2.3 MCP stdio 客户端可启动任意本地进程
- **位置**：`lib/services/mcp_client.dart` `connect()`（L70-75）：`Process.start(config.command, config.args, environment: {...Platform.environment, ...config.env})`
- **配置来源**：`Storage.loadMcpConfigJson()`（`storage.dart` L99），即 `shared_preferences` 中的 `mcpConfigJson`（含 `command/args/env`）。
- **影响**：MCP 配置等同于"允许启动任意命令行程序并继承完整环境"。当前为本地用户自行配置（设计如此），但若该 JSON 将来来自导入/远程/不可信来源，即 RCE。环境全量透传也会把 API Key 等注入子进程环境。
- **建议**：对 MCP 配置来源做签名/来源校验；最小化处理子进程环境，不要默认透传全部 `Platform.environment`。

### 2.4 巨型全局状态单例 + 生命周期/并发隐患
- **位置**：`lib/state.dart`（7,449 行，`ChangeNotifier` 全局可变状态）
  - 无 `dispose`、无 `mounted` 守卫；数百个 `async` 方法跨 `await` 并发改写共享字段（`questions`/`chatProfiles` 等），await 交错可能读到中间态或丢失更新（勘察子代理确认，无数据竞争但存在逻辑竞态）。
  - 大量 `!` 空断言：如 `m.group(1)!`、`api_service.dart:684/726/727/…`、`mcp_client.dart:182`、`dict_service.dart:312` 等——匹配成功但捕获组为 null 时会抛 `StateError`/`TypeError` 崩溃。
- **建议**：将巨型单例拆分模块；关键写入加串行化；将 `!` 改为 `?? 默认值` 或显式校验。

### 2.5 模拟考试页题号越界崩溃风险
- **位置**：`lib/widgets/exam_page.dart`（L587/598/600/602/604/606）
  - 除 `reading` 段外，`sheet.vocab[i]`、`cloze[i]`、`dialogue[i]`、`bankedCloze[i]`、`en2zh5[i]` 直接用题号索引，无长度边界检查。若 AI 生成的题量与 `sheet` 实际长度不一致，会抛 `RangeError`。
- **建议**：统一加 `i < list.length` 守卫与缺题兜底。

---

## 3. 低 / 信息（Low / Info）

### 3.1 `deepseek-harness/` 被误当子模块引入，零编译引用，体积/维护负担
- 实为普通目录拷贝（无 `.gitmodules`、`.git/config` 无记录），非真实 git 子模块。
- Flutter 项目仅把它当 UI 样式参考（注释"复刻/仿"，`lib/main.dart`、`agent_rows.dart`、`agent_service.dart`），**任何 TS/Node 源码均未被 Flutter 引用或编译**。
- 内含设计内的代码执行面（`new Function`/`eval`：`packages/extensions/cordis-client-runner/src/client/evaluator.ts:180`、`vendor/loader/src/config/utils.ts:5-7` 等）。
- 密钥扫描：仅测试夹具的 dummy 值（`sk-e2efixture1234567890` 等），无真实密钥、无恶意依赖、install 钩子 `postinstall` 仅装 git hooks（良性）。
- **结论**：非安全漏洞，但属明显的仓库卫生/维护负担（大型 monorepo 混入学习应用仓库），建议移除源码或独立私有化。

### 3.2 `config.yaml` 遗留死配置
- `config.yaml` 仅一行 `mixed-port: 7890`，**无任何 Dart 代码读取**（代理功能已在提交 `573dad1 feat: 删除代理功能` 中删除）。
- 该端口疑似 Clash 类代理的 mixed-port 残留，属误导性与维护噪声。

### 3.3 仓库卫生
- `afloat-setup.exe`（预构建、未签名）被 `git add -f` 提交（见 1.3）。
- 工作区根散落**未跟踪**构建产物：`app.so`（Linux/Android 共享对象出现在 Windows 工程根，明显错位）、`flutter_windows.dll`、`dartjni.dll`、`icudtl.dat`、`data/` 等（未被跟踪，但说明曾"构建产物进仓库根"的提交历史）。
- 根目录 `racing-game/index.html`、`snake-game/`（tsconfig+src）为遗留游戏原型，与 `lib/widgets/*_game_page.dart` 中的正式实现并存，易混淆。
- `tool/check_grammar_course.ps1`、`tool/migrate_dict.dart` 为一次性迁移/校验脚本，建议移入 `scripts/` 或文档化。

### 3.4 开发者控制台默认关闭（低风险）
- `Storage.loadDevMode()` 默认 `false`（`storage.dart` L173），故 `dev_console.dart` 的"AI 思维链"浮层默认不出现（良好）。
- 但 `devMode` 可通过 `importBackup`（L597）被外部备份开启——若用户导入他人备份，可能意外暴露内部日志。建议备份恢复时对 `devMode` 等敏感开关做提示。

### 3.5 正面项
- **FFI 绑定安全**：`native/snake_logic/snake_logic.cpp` 对渲染数组做了 `SNAKE_MAX_LEN` 钳制（`syncRenderArrays` L47、`step` L156），方向输入做了正交单位校验（`snake_turn` L249-255），`snake_snapshot` 字段与 Dart `SnakeSnapshot` 布局一致；Dart 侧对 `snakePos` 做容量钳制并拷贝到自有内存。未见越界/use-after-free。
- **JSON 解析与网络错误**：各 `jsonDecode` 基本包在 `try/catch`，API/MCP/墨墨失败均有兜底（勘察子代理确认）。
- **WebView 简单 HTML→文本**（`_toolWebFetch`）做了 script/style 剥离，降低但**不消除** HTML 解析风险（仅用于文本提取，非渲染）。
- `.gitignore` 对 `node_modules`/`dist`/DLL 等生效，仓库未膨胀。

---

## 4. 修复优先级建议

| 优先级 | 项 | 类型 |
|--------|----|------|
| P0 | 1.1 密钥明文存储 + 备份明文导出 | 机密性 |
| P0 | 1.2 收紧 Agent 默认读取半径（排除凭据路径）+ 隔离 web_fetch/search 内容 + "完全访问"显式确认 | 注入/RCE |
| P1 | 1.3 安装器去 admin 要求、签名、不在仓库放预构建 exe | 供应链 |
| P1 | 2.1 WebView 收紧 JS/弹窗、释放控制器 | 攻击面 |
| P1 | 2.2 web_fetch 禁内网/元数据地址（SSRF） | SSRF |
| P2 | 2.3 MCP 配置来源校验、最小化子进程环境 | RCE 面 |
| P2 | 2.4 state.dart 拆分 + 并发串行化 + 去除 `!` 断言 | 稳定性 |
| P2 | 2.5 exam_page 越界守卫 | 崩溃 |
| P3 | 3.1 移除/独立化 deepseek-harness；3.2 清理 config.yaml；3.3 仓库卫生 | 维护 |

---

*本报告基于静态勘察，未执行构建或动态测试；涉及运行时行为（如 shared_preferences 精确落盘路径、WebView2 版本行为）的建议已做保守表述，落地前建议以实测为准。*
